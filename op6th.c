#include<stdio.h>
int main()//6. write a program which takes a character as an input and displays its ascii 
{
    char ch;
    printf("enter the char");
    scanf("%c",&ch);
    printf("number is %d",ch);
    return 0;
}